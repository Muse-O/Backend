const express = require("express");
const router = express.Router();

const ArtgramController = require("../controllers/artgram.controller");
const adminController = new ArtgramController();

module.exports = reuter;
